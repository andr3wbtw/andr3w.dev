{
  "name": "andr3w.dev",
  "version": "1.0.0",
  "description": "",
  "main": "tailwind.config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "tailwindcss build -o dist/tailwind.css && serve",
    "build": "tailwindcss build -o dist/tailwind.css --minify"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/andr3wbtw/andr3w.dev.git"
  },
  "private": true,
  "dependencies": {
    "tailwindcss": "^3.4.11"
  },
  "devDependencies": {
    "daisyui": "^4.12.10"
  }
}